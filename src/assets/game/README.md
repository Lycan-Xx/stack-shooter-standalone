# Game asset library

This directory is the source-of-truth location for visual assets used by the
Canvas renderer. Assets are grouped by gameplay role so they can be replaced
incrementally without changing simulation code.

See `assetManifest.js` for the Phase 2 naming contract. Missing entries are
intentional until the corresponding art is produced.
